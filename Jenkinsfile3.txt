pipeline {
    agent any
    tools {
        maven "maven-principal"
    }
    stage ('Build') {
        steps {
            dir ('maven-adderapp') {
                sh 'mvn -DskipTests clean package'
            }
        }
    }
}